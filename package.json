{
  "name": "@jads-dev/jasb-tooling",
  "description": "JASB shared development dependencies.",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docker:storage": "USER_ID=$(id -u ${USER}) GROUP_ID=$(id -g ${USER}) docker compose -f develop-compose.yaml up --build --no-log-prefix storage"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.48.1",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.0",
    "eslint-plugin-simple-import-sort": "^8.0.0",
    "prettier": "^2.4.1",
    "prettier-plugin-elm": "^0.9.0",
    "typescript": "^4.4.4"
  }
}
